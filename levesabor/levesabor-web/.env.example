# FE-A03 — copiar para .env.local (nunca versionar .env.local)
# dev: mocks MSW ativos até INT-01; prod: https://app.ottimizo.ai/api/v1
NEXT_PUBLIC_API_URL=http://localhost:8080/api/v1
NEXT_PUBLIC_USE_MOCKS=true
# INT-01 — só necessário quando NEXT_PUBLIC_USE_MOCKS=false (mesmo projecto Supabase do backend,
# ver SUPABASE_JWT_ISSUER/SUPABASE_JWKS_URI em ottimizo/): Project Settings > API no dashboard.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
