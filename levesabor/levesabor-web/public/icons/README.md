# Ícones PWA (FE-A01) e logótipo

- `icon-192.png`, `icon-512.png`, `icon-maskable-512.png` — gerar a partir do **logótipo existente** (anel de 2 arcos): o SVG está inline na landing `project/Leve Sabor AI.dc.html` (nav e footer) — **reutilizar esse markup, não gerar por IA**. Fundo `#241A14`, arcos `#C43E1C` + `#E3A72E`.
- Ícones utilitários (setas, editar, lixo…) vêm da biblioteca `lucide-react` — não gerar imagens.
