// FE-D06 · T-18 Formulário de receita (criação) — ingredientes dinâmicos, passos, tags, macros (F2-ADM-05)
export default function NovaReceitaPage() {
  return <main>TODO FE-D06: POST /admin/recipes (guardar rascunho / publicar)</main>;
}
