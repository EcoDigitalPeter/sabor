// FE-D04 · T-15 Formulário de produto (edição) — F2-ADM-03
export default function ProdutoDetalhePage({ params }: { params: { id: string } }) {
  return <main>TODO FE-D04: PUT /admin/products/{params.id} + PUT .../prices/(storeId)</main>;
}
