// FE-D05 · T-16 Import Excel — upload → pré-visualização com erros → confirmação → resultado (F2-ADM-04)
export default function ImportarPage() {
  return <main>TODO FE-D05: POST /admin/products/import → confirm; links template/export</main>;
}
