// FE-D03 · T-13 Formulário de loja (edição) + tab de preços inline — F2-ADM-02/03
export default function LojaDetalhePage({ params }: { params: { id: string } }) {
  return <main>TODO FE-D03: PUT /admin/stores/{params.id} + preços por loja</main>;
}
