// FE-C04 · T-07 A gerar plano — polling + mensagens rotativas (F1-CLI-02)
// Ilustração animada: /images/geracao-ia (P-03) — prompt na pasta.
export default function GerarPlanoPage() {
  return <main>TODO FE-C04: POST /me/meal-plans → polling GET /me/meal-plans/(id) até READY|FAILED</main>;
}
