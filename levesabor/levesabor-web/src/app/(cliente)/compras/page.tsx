// FE-C06 · T-06 Lista de compras — grupos por categoria (ícones P-06), checkboxes otimistas (F1-CLI-06)
// Empty state: /images/empty-compras (P-04) — prompt na pasta.
export default function ComprasPage() {
  return <main>TODO FE-C06: GET /me/shopping-list + PATCH items/(id)</main>;
}
