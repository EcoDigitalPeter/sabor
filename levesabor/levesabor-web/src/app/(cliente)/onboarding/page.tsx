// FE-C02 · T-03 Onboarding do perfil — wizard 5 passos + resumo (F1-CLI-01)
// Ilustrações: /images/onboarding (P-01) e /images/onboarding-sucesso (P-05) — prompts nas pastas.
export default function OnboardingPage() {
  return <main>TODO FE-C02: wizard (objetivo → condição → alergias → orçamento → refeições/dia → resumo)</main>;
}
