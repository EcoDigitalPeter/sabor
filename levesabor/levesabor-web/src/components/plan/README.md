# components/plan — componentes do portal cliente (usados por FE-C03/C05/C06)

- `MealCard.tsx` — cartão de refeição: nome (font-display), chips `620 kcal · 35 min`, MacroRing `sm`.
- `DayTabs.tsx` — tabs seg–dom com scroll horizontal, dia atual pré-selecionado.
- `DaySummary.tsx` — total kcal + macros do dia.
- `SwapSheet.tsx` — bottom-sheet da troca de refeição (FE-C05).
- `ShoppingGroup.tsx` — grupo colapsável por categoria com ícone P-06 (FE-C06).
