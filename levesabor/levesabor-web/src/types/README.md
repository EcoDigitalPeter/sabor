# types — gerados, não escrever à mão (MOCK-03)

`api.d.ts` é gerado por `npm run gen:types` a partir do OpenAPI do backend (`levesabor-api/openapi.yaml`, cartão MOCK-01).
O CI verifica que está atualizado. Nunca editar manualmente.
